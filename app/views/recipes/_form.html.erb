<%=form_for(@recipe) do |f|%>
  <% if f.object.errors.any? %>
    <% f.object.errors.full_messages.each do |message|%>
      <%= message %><br>
    <%end%>
    <br>
  <%end%>
  

  Name: <%= f.text_field :name%><br>

  Select Ingredients:<br>
  <%= f.collection_select :ingredients, Ingredient.all, :id, :name_with_serving_info,:include_blank => true %><br>
  <!--want form to outout something like recipe[ingredient_ids][ids of selected ingredients]-->
  <!--right now nothing comes through in the params hash when I select existing ingredients-->
  <!-- I'm getting recipe[ingredients][nothing]-->
  <%= f.collection_select :ingredients, Ingredient.all, :id, :name_with_serving_info,:include_blank => true %><br>
  <%= f.collection_select :ingredients, Ingredient.all, :id, :name_with_serving_info,:include_blank => true %><br>
  <%= f.collection_select :ingredients, Ingredient.all, :id, :name_with_serving_info,:include_blank => true %><br>
  <%= f.collection_select :ingredients, Ingredient.all, :id, :name_with_serving_info,:include_blank => true %><br>



  Instructions: 
  <%= f.text_area :instructions, placeholder: "1. Mix dry ingredients, 2. Mix wet ingredients, . . . "%><br>
  <%=f.submit%>
<%end%>