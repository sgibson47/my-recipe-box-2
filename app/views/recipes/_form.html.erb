<%=form_for(@recipe) do |f|%>
  <% if f.object.errors.any? %>
    <% f.object.errors.full_messages.each do |message|%>
      <%= message %><br>
    <%end%>
    <br>
  <%end%>
  

  Name: <%= f.text_field :name%><br>

  Select Ingredients:<br>
  <%= f.collection_select :ingredient_ids, Ingredient.all, :id, :name_with_serving_info,:include_blank => true %><br>
  <!--want form to outout something like recipe[ingredient_ids][ids of selected ingredients]-->
  <!-- Now "recipe"=>{ . . . "ingredient_ids"=>"" . . . } after selecting 2 ingredients-->
  <%= f.collection_select :ingredient_ids, Ingredient.all, :id, :name_with_serving_info,:include_blank => true %><br>
  <%= f.collection_select :ingredient_ids, Ingredient.all, :id, :name_with_serving_info,:include_blank => true %><br>
  <%= f.collection_select :ingredient_ids, Ingredient.all, :id, :name_with_serving_info,:include_blank => true %><br>
  <%= f.collection_select :ingredient_ids, Ingredient.all, :id, :name_with_serving_info,:include_blank => true %><br>



  Instructions: 
  <%= f.text_area :instructions, placeholder: "1. Mix dry ingredients, 2. Mix wet ingredients, . . . "%><br>
  <%=f.submit%>
<%end%>